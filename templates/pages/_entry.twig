{% if entry is not defined %}
    {% set entry = craft.entries.section('pages').slug('home').one() %}
{% endif %}

{% extends "_layout" %}

{% block content %}
    {% for block in entry.blocks.all() %}
        {% include "_blocks/" ~ block.type with { block: block } %}
    {% endfor %}
{% endblock %}